# Solarization Planner (Vue 3 + TypeScript + Vite)

Runs 100% in the browser (no backend). Designed for **VS Code in GitHub Codespaces**.

## Quick start (Codespaces)
1. Open this repo in Codespaces (or click **Code → Create codespace**).
2. After the container starts, run:
   ```bash
   npm run dev
